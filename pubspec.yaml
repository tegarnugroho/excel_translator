name: excel_translator
description: Generate type-safe Flutter localizations from Excel, CSV, and ODS
  files with multi-sheet support and automatic validation.
version: 2.1.3
homepage: https://github.com/tegarnugroho/excel_translator
repository: https://github.com/tegarnugroho/excel_translator
issue_tracker: https://github.com/tegarnugroho/excel_translator/issues
documentation: https://github.com/tegarnugroho/excel_translator#readme

topics:
  - localization
  - excel
  - spreadsheet
  - csv
  - ods

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  build: ^4.0.4
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  glob: ^2.1.0
  path: ^1.9.1
  table_parser: ^1.0.1
  yaml: ^3.1.2

dev_dependencies:
  build_config: ^1.1.0
  build_runner: ^2.4.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  test: ^1.24.0

executables:
  excel_translator: excel_translator
